# Optional: backend API base URL (no trailing slash).
# Leave unset or empty to use relative /api (same host + Vite proxy in dev).
# Example for a separate backend: https://localhost:8080
# VITE_API_BASE_URL=
